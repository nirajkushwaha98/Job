export default function Applications({ applications }) {
  return <section id="applications" className="py-5"><div className="container"><div className="section-heading mb-4"><span className="text-primary fw-semibold">APPLICATIONS</span><h2 className="mt-2">Your application tracker</h2><p className="text-secondary">Keep an eye on roles you have applied for.</p></div>{applications.length ? <div className="row g-3">{applications.map((job) => <div className="col-md-6 col-lg-4" key={job.id}><div className="application-card"><div className="company-mark">{job.company[0]}</div><div className="flex-grow-1"><h3 className="h6 mb-1">{job.title}</h3><p className="text-secondary small mb-1">{job.company} · {job.location}</p><span className="badge text-bg-success">Applied</span></div></div></div>)}</div> : <div className="empty-applications text-center rounded-4 p-5"><i className="bi bi-send fs-1 text-primary" /><h3 className="h5 mt-3">No applications yet.</h3><p className="text-secondary mb-3">Find a role that excites you and send your first application.</p><a href="#jobs" className="btn btn-primary">Browse Jobs</a></div>}</div></section>;
}
