export default function JobCard({ job, applied, onApply }) {
  return <article className="job-card card h-100 border-0 shadow-sm"><div className="card-body p-4 d-flex flex-column"><div className="d-flex justify-content-between align-items-start gap-3"><span className="company-mark">{job.company[0]}</span><span className="badge category-badge">{job.category}</span></div><h3 className="h5 mt-3 mb-1">{job.title}</h3><p className="text-primary fw-semibold small mb-3">{job.company}</p><div className="job-meta text-secondary small mb-3"><span><i className="bi bi-geo-alt me-1" />{job.location}</span><span><i className="bi bi-clock me-1" />{job.type}</span><span><i className="bi bi-cash-stack me-1" />{job.salary}</span></div><p className="text-secondary small flex-grow-1">{job.description}</p><button className={`btn ${applied ? 'btn-success' : 'btn-primary'} w-100 mt-2`} disabled={applied} onClick={() => onApply(job)}>{applied ? <><i className="bi bi-check2 me-1" />Applied</> : <>Apply Now <i className="bi bi-arrow-right ms-1" /></>}</button></div></article>;
}
