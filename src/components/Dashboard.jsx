export default function Dashboard({ jobCount, applicationCount }) {
  const cards = [{ icon: 'bi-person', label: 'Profile', value: 'Complete your profile', color: 'primary' }, { icon: 'bi-briefcase', label: 'Available Jobs', value: `${jobCount} open opportunities`, color: 'success' }, { icon: 'bi-send', label: 'Applications', value: `${applicationCount} submitted`, color: 'warning' }];
  return <section className="dashboard-strip py-5"><div className="container"><div className="d-flex justify-content-between align-items-end mb-4"><div><span className="text-primary fw-semibold small">YOUR DASHBOARD</span><h2 className="h3 mb-0 mt-1">Keep your career moving</h2></div><a href="#jobs" className="small text-decoration-none">Explore roles <i className="bi bi-arrow-right" /></a></div><div className="row g-3">{cards.map((card) => <div className="col-md-4" key={card.label}><div className="summary-card"><div className={`summary-icon text-${card.color} bg-${card.color}-subtle`}><i className={`bi ${card.icon}`} /></div><div><span className="text-secondary small">{card.label}</span><strong className="d-block">{card.value}</strong></div><i className="bi bi-chevron-right ms-auto text-secondary" /></div></div>)}</div></div></section>;
}
